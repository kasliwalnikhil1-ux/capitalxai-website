---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';
---

<BaseLayout
  title="Pricing – CapitalxAI"
  description="CapitalxAI pricing plans. Your IR reps at a fraction of the cost. Fully managed investor relations starting at $300/month."
  canonical="/pricing"
>
  <Header />
  <main id="main-content" class="pricing-page">
    <section class="pricing-hero">
      <div class="pricing-glow" aria-hidden="true"></div>
      <div class="container">
        <h1 class="pricing-headline">
          <span class="headline-white">Your IR reps at a</span>
          <span class="headline-gradient">fraction of the cost</span>
        </h1>

        <div class="pricing-grid">
          <!-- Left: Fully managed -->
          <div class="pricing-left">
            <h2 class="managed-title">Fully managed</h2>
            <p class="managed-desc">
              By taking over the mundane aspects of fundraising, CapitalxAI empowers you to concentrate on what truly matters — forging lasting investor partnerships and bringing visionary projects to fruition.
            </p>
            <p class="managed-desc">
              Our ethos is centered around amplifying human capabilities, allowing founders to focus on relationship-building and strategic thinking - elements that machines can't replicate.
            </p>
          </div>

          <!-- Right: Pricing card -->
          <div class="pricing-card">
            <h3 class="card-title">Superhero for investor relations</h3>
            <p class="card-desc">
              CapitalxAI serves as your investor relations (IR) representative, business analyst, creative writer, and outreach specialist, all four combined, to book meetings for you with mission-aligned investors.
            </p>
            <div class="card-price">
              <span class="price-label">Starts at</span>
              <span class="price-amount">$ 500</span>
              <span class="price-period">/month</span>
            </div>
            <p class="card-custom">
              Contact us for custom pricing based on your stage and sector.
            </p>
            <a href="https://calendly.com/founders-capitalxai/20min" class="card-cta" target="_blank" rel="noopener noreferrer">
              Start Fundraising
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</BaseLayout>

<style>
  .pricing-page {
    background: #0b1020;
    min-height: 100vh;
  }

  .pricing-hero {
    position: relative;
    padding-block: 5rem 6rem;
    overflow: hidden;
  }

  .pricing-glow {
    position: absolute;
    left: -10%;
    bottom: -20%;
    width: 70%;
    height: 80%;
    background: radial-gradient(
      ellipse at center,
      rgba(255, 200, 100, 0.25) 0%,
      rgba(255, 140, 180, 0.2) 25%,
      rgba(180, 100, 255, 0.2) 50%,
      rgba(100, 180, 255, 0.15) 75%,
      transparent 100%
    );
    filter: blur(80px);
    pointer-events: none;
  }

  .pricing-headline {
    text-align: center;
    margin: 0 0 4rem;
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.02em;
  }

  .headline-white {
    display: block;
    color: rgba(255, 255, 255, 0.98);
  }

  .headline-gradient {
    display: block;
    background: linear-gradient(
      90deg,
      rgba(255, 255, 255, 0.5) 0%,
      rgba(200, 200, 220, 0.4) 100%
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: start;
  }

  @media (min-width: 900px) {
    .pricing-grid {
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
    }
  }

  /* Left side: Fully managed */
  .pricing-left {
    position: relative;
    z-index: 1;
  }

  .managed-title {
    margin: 0 0 1.5rem;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    color: rgba(255, 255, 255, 0.98);
    letter-spacing: -0.02em;
  }

  .managed-desc {
    margin: 0 0 1.25rem;
    font-size: clamp(1rem, 1.5vw, 1.125rem);
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.7;
  }

  /* Right side: Pricing card */
  .pricing-card {
    position: relative;
    z-index: 1;
    background: rgba(22, 30, 52, 0.85);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 1rem;
    padding: 2.5rem;
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.3);
  }

  .card-title {
    margin: 0 0 1rem;
    font-size: clamp(1.25rem, 2vw, 1.5rem);
    font-weight: 700;
    color: rgba(255, 255, 255, 0.95);
  }

  .card-desc {
    margin: 0 0 1.5rem;
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.65;
  }

  .card-price {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }

  .price-label {
    font-size: 0.9375rem;
    color: rgba(255, 255, 255, 0.6);
  }

  .price-amount {
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 700;
    color: rgba(255, 255, 255, 0.98);
    letter-spacing: -0.02em;
  }

  .price-period {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.6);
  }

  .card-custom {
    margin: 0 0 1.5rem;
    font-size: 0.9375rem;
    color: rgba(255, 255, 255, 0.6);
    line-height: 1.5;
  }

  .card-cta {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    width: 100%;
    padding: 0.875rem 1.5rem;
    background: rgba(117, 126, 146, 0.9);
    color: rgba(255, 255, 255, 0.96);
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 9999px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.22);
    transition: transform 0.15s ease, opacity 0.2s ease;
  }

  .card-cta:hover {
    transform: translateY(-1px);
    opacity: 0.95;
  }

  .cta-arrow {
    font-size: 1.1em;
  }

  @media (max-width: 768px) {
    .pricing-hero {
      padding-block: 3rem 4rem;
    }

    .pricing-card {
      padding: 2rem 1.5rem;
    }

    .pricing-glow {
      left: -30%;
      bottom: -10%;
      width: 100%;
      height: 60%;
    }
  }
</style>
